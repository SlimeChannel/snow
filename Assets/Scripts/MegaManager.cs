namespace snow
{
    using UnityEngine;
    public class MegaManager : MonoBehaviour
    {
        [SerializeField] GameObject networkManager;
        void Start()
        {
            // networkManager.GetComponent<Transform>().StartHost();
        }
    }
}